apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ip-app-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ip-app-service
                port:
                  number: 80

